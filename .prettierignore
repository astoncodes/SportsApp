node_modules/
dist/
build/
.expo/
coverage/

# Generated — regenerate with `npm run db:types`, never hand-format.
packages/database-types/src/database.types.ts

# Generated by the Expo CLI. Gitignored, and the file itself says not to edit
# it — but prettier reads .prettierignore, not .gitignore, so it needs naming
# here or `format:check` fails on a file nobody owns.
apps/mobile/expo-env.d.ts

# Lockfiles
package-lock.json

# SQL: prettier has no Postgres parser and would mangle these.
supabase/migrations/
supabase/tests/

assets/
